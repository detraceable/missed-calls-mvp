---
description: Setup Omni-Comm SMS Engine
---

// turbo-all

1. Install required dependencies (Twilio, Stripe, Postgres).
`npm install twilio stripe postgres`

2. Ensure migration directories exist.
`mkdir -p supabase/migrations`
